const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const wedding = await prisma.wedding.findFirst({ where: { slug: 'joao-e-maria' } }); const guest = await prisma.guest.create({ data: { weddingId: wedding.id, name: 'Tio Patinhas', email: 'tio@patinhas.com', token: 'PAT123', qrCode: 'QR-PAT123' } }); console.log(guest); } main().finally(() => prisma.$disconnect());
